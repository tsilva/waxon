# Remove legacy source data with a clean break

Waxon will delete legacy documents, source material, provenance, coverage, migration-only storage, and the code that supports them instead of preserving a staged compatibility path. The clean break accepts destructive migration work because retaining the former source-oriented model would contradict Waxon's question-only product boundary and prolong complexity that has no supported use.
